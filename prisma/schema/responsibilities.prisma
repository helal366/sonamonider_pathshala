model Responsibility {
  responsibility_id   String   @id @default(uuid())
  responsibility_name String   @unique


  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  @@map("responsibilities")
}
